你是一位农业试验小区划分专家。分析这幅无人机影像的局部区域，识别关键特征点。

{{image_info}}

用户绘制的示例区域大小: {{user_width}}x{{user_height}} 像素

任务：
1. 识别田块的角点（田块边界的拐角，最多4个）
2. 识别小区之间的分隔线端点（道路、沟渠等的端点）
3. 坐标需要加上区域偏移量: x + {{offset_x}}, y + {{offset_y}}

输出格式（纯JSON）：
{
  "field_corners": [
    {"x": 100, "y": 200},
    {"x": 300, "y": 200},
    {"x": 300, "y": 400},
    {"x": 100, "y": 400}
  ],
  "separators": [
    {"points": [{"x": 100, "y": 300}, {"x": 300, "y": 300}]},
    {"points": [{"x": 200, "y": 200}, {"x": 200, "y": 400}]}
  ]
}

规则：
- 坐标是像素坐标，原点在区域左上角
- 只输出你能从影像中实际看到的特征点
- 如果看不清楚，少输出也比乱输出好
- 只输出JSON，无其他文字
